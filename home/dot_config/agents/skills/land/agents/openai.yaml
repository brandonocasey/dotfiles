# Codex counterpart of the `disable-model-invocation` frontmatter in SKILL.md:
# merging into the default branch is the user's call, so only an explicit $land runs it.
policy:
  allow_implicit_invocation: false
