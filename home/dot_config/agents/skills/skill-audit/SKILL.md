---
name: skill-audit
description: >
  Renamed to llm-setup-audit; kept only as an alias so /skill-audit still works.
  Use only when the user invokes /skill-audit.
---

This skill was renamed to `llm-setup-audit`. Invoke that skill (Skill tool,
`skill: "llm-setup-audit"`, pass any arguments through) and follow it — it is the
single authority for the audit flow.
