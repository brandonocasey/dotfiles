# Codex counterpart of the `disable-model-invocation` frontmatter in SKILL.md:
# pushing and opening an MR/PR is the user's call, so only an explicit $ship runs it.
policy:
  allow_implicit_invocation: false
