#!/usr/bin/env bash
cat << EOF
Cheatsheet:
  * choose    - Use instead of cut/awk | * curlie  - curl with colors
  * dasel     - json/toml/yaml/xml/csv | * delta   - diff tool
  * duf       - df, list drive info    | * dust    - du, files/dir size as tree
  * entr      - watch change, run task | * fd      - find alternative
  * forgit    - TODO                   | * fq      - parse binary formats
  * gh        - github cli             | * git-ext - TODO
  * glow      - cli md previews        | * grex    - generate regex from tests
  * hyperfine - benchmark cli          | * lazygit - TODO
  * parallel  - xargs but parallel     | * tldr    - cheatsheets for commands
  * tokei     - github style loc/lang  | * s       - web search from cli
  * sd        - sed with better regex  | * xh      - http requests
  * zi        - fzf cd using zoxide    | *

Poweline git symbol info: <branch> <ongoing action> ⇣1 ⇡2 *3 ~4 +5 !6 ?7
  ⇣1 - commits behind remote  | ⇡2 - commits ahead of the remote
  *3 - stashes                | ~4 - merge conflicts
  +5 - staged changes         | !6 - unstaged changes
  ?7 - untracked files        |

Keybinds:
EOF
