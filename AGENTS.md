# Chezmoi repository

For work in this repository, skip the `worktree` skill and use the primary
checkout. Do not create a worktree. This overrides the global worktree rule.

Keep the normal commit, land, and push authorization rules. Preserve unrelated
files and edits.
